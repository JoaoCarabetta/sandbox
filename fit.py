import subprocess
from itertools import cycle

subprocess.Popen(["sleep", "30"])
subprocess.Popen(["touch", "test.fit2"])


def linha(pipe):

    return delay


def main():

    for i in cycle([1, 2]):

        delay = linha(pipe=pipe)

        # # wait(delt)

        # pipe += 1


a = "ola {var1} }} joao"
print(a.format(var1="teste"))
